# This is a Databricks asset bundle definition for Databricks_medallion_architecture.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: Databricks_medallion_architecture
  uuid: 30494d48-c734-4182-a507-9395da4e1b86

include:
  - resources/*.yml
  - resources/*/*.yml

# Variable declarations. These variables are assigned in the dev/prod targets below.
variables:
  catalog:
    default: "citibike_dev"
    description: The catalog to use
  schema:
    default: "public"
    description: The schema to use
  dev_flag:
    default: true


targets:

  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: production # changed from development to production
    variables:
      catalog:
        default: "citibike_test"
      dev_flag:
        default: false
    presets:
      name_prefix: '[development] '
    workspace:
      host: https://dbc-7ccec0a9-5a27.cloud.databricks.com # replace with your Workspace URL
      root_path: /Workspace/Shared/.bundle/${bundle.name}/${bundle.target}
    permissions:
      - user_name: lakshaykumarpandit@gmail.com # update with your service principal name
        level: CAN_MANAGE
    run_as:
      user_name: lakshaykumarpandit@gmail.com # update with your service principal name

  test:
    mode: production
    variables:
      catalog:
        default: "citibike_test"
      dev_flag:
        default: false
    presets:
      name_prefix: '[testing] '
    workspace:
      host: https://dbc-7ccec0a9-5a27.cloud.databricks.com # replace with your Workspace URL
      root_path: /Workspace/Shared/.bundle/${bundle.name}/${bundle.target}
    permissions:
      - user_name: lakshaykumarpandit@gmail.com # update with your service principal name
        level: CAN_MANAGE
    run_as:
      user_name: lakshaykumarpandit@gmail.com # update with your service principal name
  prod:
    mode: production
    variables:
      catalog:
        default: "citibike_prod"
      dev_flag:
        default: false
    workspace:
      host: https://dbc-7ccec0a9-5a27.cloud.databricks.com # replace with your Workspace URL
      root_path: /Workspace/Shared/.bundle/${bundle.name}/${bundle.target}
    permissions:
      - user_name: lakshaykumarpandit@gmail.com # update with your service principal name
        level: CAN_MANAGE
    run_as:
      user_name: lakshaykumarpandit@gmail.com # update with your service principal name